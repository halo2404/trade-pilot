// Shared React UI components — to be extracted from apps/web/src/components/ui
// as the monorepo matures.
//
// Usage:  import { Button } from "@tradepilot/ui"
//
// Currently a stub. Components live in apps/web/src/components/ui until they
// are needed by a second consumer (e.g. a future admin app or mobile web shell).

export {};
